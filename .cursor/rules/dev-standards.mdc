---
description: 通用開發規範 — 核心原則、思考流程、實作慣例與輸出格式。
alwaysApply: true
---

# 核心原則

- **簡潔優先**：偏好可讀、直觀的邏輯，避免過度設計。
- **單一職責**：每個函式或類別只做一件事。
- **錯誤處理**：所有外部輸入與 I/O 操作都必須有明確的 try/catch 與優雅降級。

# 寫程式前的思考流程

1. **情境分析**：先了解現有的檔案結構與引入習慣，絕不引入專案中不存在的套件。
2. **改動評估**：修改時採用最小改動原則，保留現有穩定邏輯。
3. **安全檢查**：掃描潛在漏洞，例如硬編碼的 API key、SQL injection 風險、或暴露的機密資料。

# 實作規範

### 註解
```ts
// ❌ 錯誤 — 描述行為（what）
// 遞增計數器
count++

// ✅ 正確 — 說明原因（why）
// 達到重試上限，遞增以觸發熔斷機制
count++
```

### 命名
- 使用能表達意圖的描述性名稱，避免 `data`、`info`、`temp`、`val`。
- 遵循語言慣例：Python 用 `snake_case`，TypeScript/JavaScript 用 `camelCase`。

```ts
// ❌ 錯誤
const data = await api.get('/users')

// ✅ 正確
const activeUsers = await api.get('/users')
```

### 測試
新增功能時，主動在實作前或實作時一併提出對應的單元測試案例。

# 輸出格式

- **不廢話**：跳過基本語法說明，直接給出修正或程式碼變更。
- **區塊式編輯**：對於長檔案，只顯示受影響的區塊，並標註其所在位置。
- **一次一件事**：任務複雜時，拆成步驟逐一確認後再進行。
